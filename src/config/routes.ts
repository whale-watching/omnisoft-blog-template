/**
 * @description Enum containing routes to all pages rendered by react-router
 */
export enum Routes {
  Root = "/",
  Blog = "/blog",
  Privacy = "/privacy-policy",
  Easy = "/easy",
  Medium = "/medium",
  Hard = "/hard",
  Expert = "/expert",
  Extreme = "/extreme",
}
